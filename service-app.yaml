apiVersion: v1
kind: Service
metadata:
  name: app-service-orders
spec:
  selector:
    app: fiap-orders-app
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3000
      nodePort: 30007
  type: NodePort
